{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Module Mode Launch",
            "type": "debugpy",
            "request": "launch",
            "module": "evaluator.align", // 替换为实际的模块名
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
        }
    ]
}